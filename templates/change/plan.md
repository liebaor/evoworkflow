---
change: CHANGE-ID
status: DRAFT
approval: null
currentTruthTargets: []
---

# Implementation plan / 实施计划

## Reuse analysis / 复用分析

## Primary and affected modules / 主模块与受影响模块

## Expected blast radius / 预计影响范围

## Vertical Slices / 垂直 Slice

### S1 — User-visible behavior

- Objective / 目标：
- Acceptance / 验收：
- Expected paths / 预期路径：
- Verification / 验证：
- Dependencies / 依赖：

## Unaffected behavior / 不受影响的行为

## Current truth targets / 当前事实目标

在这里维护 Plan frontmatter 的 `currentTruthTargets`：用 `CREATE` 或 `UPDATE` 声明 Finish 后必须存在的仓库路径，用 `UNAFFECTED` 声明已确认不受影响的路径。没有声明的旧版 Plan 会保留兼容，但 Doctor 会提示补齐。

## Migration and rollback / 迁移与回滚

## Stop conditions / 停止条件
