{
  "name": "@evoworkflow/cli",
  "version": "0.2.0",
  "private": true,
  "description": "Human-controlled, repository-centered AI software engineering workflow",
  "type": "module",
  "bin": {
    "evo": "./dist/index.js"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.19.0",
  "oclif": {
    "bin": "evo",
    "dirname": "evoworkflow",
    "commands": "./dist/commands",
    "topicSeparator": " "
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build && pnpm run smoke:cli && pnpm run validate:skills && pnpm run check:schemas && pnpm run eval:hardening",
    "check:schemas": "tsx scripts/check-schemas.ts",
    "dev": "tsx src/index.ts",
    "evo": "tsx src/index.ts",
    "eval:ruoyi": "tsx scripts/phase2-ruoyi-smoke.ts",
    "eval:hardening": "tsx scripts/hardening-evals.ts",
    "eval:phase2": "tsx scripts/phase2-evals.ts",
    "generate:schemas": "tsx scripts/generate-schemas.ts",
    "smoke:cli": "tsx scripts/smoke-cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate:skills": "tsx scripts/validate-skills.ts"
  },
  "dependencies": {
    "@oclif/core": "^5.0.0",
    "yaml": "^2.9.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
