# Codex native Skills

Codex is the default EVO Harness. The canonical runtime path is:

```text
~/.agents/skills/
```

No `.codex/skills` duplicate is created. The repository still supplies `AGENTS.md`, `.evo/`, Change, Plan, Evidence, and Git as the engineering authority.

Selected entry Skills include `agents/openai.yaml` for Codex presentation and invocation policy. This metadata does not grant lifecycle authority; approval, `--apply`, commit, and Finish gates remain in the repository protocol.
